services:
  - type: web
    name: smart-agricultural-production
    env: python
    buildCommand: pip install -r requirements.txt && python utils/train.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
